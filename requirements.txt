# RAG from Scratch — dependencies
# For uv users:  uv sync   (uses pyproject.toml / uv.lock instead of this file)
# For pip users: pip install -r requirements.txt

# --- API + server ---
fastapi>=0.138.0
uvicorn>=0.49.0
python-multipart>=0.0.32      # required for file uploads (UploadFile)

# --- LLM + embeddings (Ollama) ---
ollama>=0.6.2
langchain>=1.3.11
langchain-core>=1.4.8
langchain-community>=0.4.2
langchain-ollama>=1.1.0
langchain-text-splitters>=1.1.2

# --- vector store ---
chromadb>=1.5.9
faiss-cpu>=1.14.3

# --- PDF reading ---
pypdf>=6.14.2
pymupdf>=1.27.2.3
pypdfium2>=5.10.1

# --- embeddings / ML (used by notebooks + main_test.py) ---
sentence-transformers>=5.6.0
scikit-learn>=1.6.0
torch>=2.12.1
torchtext>=0.6.0

# --- notebooks / misc ---
ipywidgets>=8.1.8
rich>=15.0.0
